This branch was the first attempt on rewriting RocketMod but it failed due inactivity and unexpected problems.
We created another rework which was later merged to master.

This branch is read only and only kept for archive purposes; after porting some missing features from here to master it will likely be deleted.
